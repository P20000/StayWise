the content that is currently inside the frontend ui right now looks like it is hard coded, so make sure to scrape it out and only keep the original right content that will come as the vendors put their ads on it. and they would properly make entire thing populated. i want you to make sure that the current landing page itself is in proper viewable position with no missing photos whatsoever. 

then inside the login and log out page ,there should be 2 options, where the first thing would be to make the vendor login system. although the login system would be the same but the setup system would differ.

for the vendors set up page, it would ask for a location that would be tracked thorugh a locatio tracking api. then that location will be stored for the vendor and everytime a user will search for a location, the nearest locations will be populated.

vendor would have all the options of adding removing and managing the entire hotel's stuff within the application. and the rating system within the application will be directly affecting the vendor's "ad" rating. that would be easily look-able by the users


